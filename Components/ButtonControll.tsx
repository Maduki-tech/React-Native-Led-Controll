import React from 'react'
import { View, Text, Button } from 'react-native'

export default function ButtonControll() {
    return (
        <View>
            <Button title='Led Steuern' onPress={() => {} } />
            <Button title='Led Hinzufügen' onPress={() => {} } />
        </View>
    )
}
